{
  "private": true,
  "name": "d3-world-map",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch",
    "lint": "eslint src",
    "test": "jest"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  },
  "devDependencies": {
    "eslint": "^8.27.0",
    "eslint-config-preact": "^1.3.0",
    "preact-cli": "^2.2.1",
    "sirv-cli": "^2.0.2"
  },
  "dependencies": {
    "d3": "^7.6.1",
    "leaflet": "^1.9.2",
    "preact": "^10.11.2",
    "preact-render-to-string": "^5.2.6"
  }
}
